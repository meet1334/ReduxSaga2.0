# JSON DATA SERVER

- npm i -g json-server (Globally install package)

- create db folder and create json data

- Run this json using this command:
  

  - json-server --watch db.json --port 3500 (without --port it will run on 3000 port )

